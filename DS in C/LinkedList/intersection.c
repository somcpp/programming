#include<stdio.h>
struct node{
    int data;
    struct node* next;
};
typedef struct node* NODE;

NODE createAndNodes(NODE first) {
    NODE newNode = (NODE)malloc(sizeof(struct node));
    int x
    if(first==NULL){

    }
}

void main(){

}