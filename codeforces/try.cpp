#include <bits/stdc++.h>
using namespace std;

#define fast ios::sync_with_stdio(false); cin.tie(nullptr);
typedef long long ll;

int main(){
    fast;
    int t; cin >> t;
    while(t--){
        
    }
    return 0;
}